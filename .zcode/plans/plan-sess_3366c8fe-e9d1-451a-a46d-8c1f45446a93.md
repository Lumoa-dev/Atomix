# 运行时策略模块 — 仿真验证与文档重构

## 执行内容

### 1. 仿真框架 (`sim/`)
- Python 离散事件仿真，模拟任务到达→任务池→批次调度→槽位执行→完成回收全流程
- 俄罗斯方块内存模型（槽位+滑道+死区回收）
- 多算法变体对比：因子平滑化(4种) × 合并策略(3种) × OOM反馈(3种) × 滑道尺寸(3种)
- 6个测试场景：稳态/突发/内存压/CPU压/震荡/长跑
- matplotlib 可视化报告

### 2. 文档重构
- `运行时架构.md` 精简为纯机制文档
- 新建 `策略模块.md` 描述 AdaptiveResourceController 算法设计

### 3. 交付物
- sim/ 完整仿真代码 + 运行结果 + 可视化报告
- 更新后的文档